import { FC } from 'react'

const Loading: FC = () => {
    return (
        <span className="block size-6 animate-spin rounded-full border-4 border-zinc-500/50 border-t-zinc-50"></span>
    )
}

export default Loading
